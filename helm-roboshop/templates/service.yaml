apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.appName }}
  namespace: roboshop
spec:
  type: {{.Values.service.type }}
  selector:
    app: {{ .Values.appName }}
  ports:
    - protocol: TCP
      port: {{ .values.service.servicePort }}
      targetPort: {{ .Values.service.containerPort }}